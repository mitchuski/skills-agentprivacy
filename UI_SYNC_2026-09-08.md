# Skills display and cohort sync — 8 September 2026

Local changes, not yet committed or deployed.

The hosted display source is this repo (mitchuski/skills-agentprivacy); persona and role content lives in the sibling agentprivacy-skills repo. Updated bin/build-static.js and rendered site/index.html with shared dark palette, serif headings, navigation to City/Labs, orbital lattice decoration, section anchors, mobile controls and the floating path tray. Reduced motion is respected. Catalogue requests bypass stale browser caches. Non-farm previews use the snapshot immediately rather than probing private services.

bin/sync-city-star.js updates only the explicitly circulating City/Star manifest cohort, plus the existing compression-defence packet whose shipped body was corrupted and no longer matched its declared digest. It requires the local publication filters and PII replacement file; it does not expose their values. It validates inputs before writing, preserves unrelated packets and adoption fields, and publishes body hashes over the resulting bytes. Source addresses identify the skill repository. No private drafts or credentials are harvested. The new city-star-arrival deck includes eight operational roles; seven persona packets are refreshed. The Star Companion now resolves the current star-key source.

Run node bin/sync-city-star.js with the sibling agentprivacy-skills repository present. Run node bin/build-starchart.js --snapshot-only to update chart source relations and decks while retaining the existing runtime snapshot and avoiding network/farm writes. The template was rendered in isolation for this pass rather than running the full build-static publisher, which also contacts the librarian and mirrors the farm.

Validation: 202 published body hashes match; all deck packet references resolve. Chart: 202 stars, 228 source/deck edges, ten constellations. Source template and shipped index agree. Browser confirmed snapshot loading, mobile layout and Star Companion search (one match out of 202). A node or shared deck edge does not attest trust. Older persona research citations remain source-authored historical references, not a fresh register audit.

Release boundary: review content and push agentprivacy-skills before or alongside this display package. Guide's D4 presentation is a separate repository; Star's registry adapter remains an implementation with transport fixtures, not a live community registry. Do not run a full farm build or deploy hosting implicitly when publishing these static changes.
